---
name: "部署环境"
description: "宿主环境：作为标准 ST 扩展跑在主文档，含 CSS 层叠上下文约束。改任何涉及 window/document/clipboard/挂载点定位/拖拽监听器绑定/CSS 选择器的代码，或排查'设置不生效/拖拽没反应/复制失败/样式被覆盖'类症状时读。"
kind: "guide"
alwaysApply: false
---

# 部署环境

## 部署环境：标准 ST 扩展，跑在主文档

1. 项目打包成 `dist/index.js`（ESM，`vite.config.js` 用 `lib` + `es` format）+ `dist/style.css`（独立 CSS 产物）。
2. `manifest.json` 声明 `js`/`css`/`hooks`，由 ST 扩展管理器加载——**脚本直接跑在 ST 主文档的 ES module 上下文里**，不是 iframe。
3. `hooks.activate → init()` 是 ST 激活扩展时调的入口；`hooks.install/update → refresh()` = `location.reload()`。
4. **后果**：`window`/`document` 就是主文档的，没有 iframe 分裂。裸 `window.xxx`/`document.xxx` 直接可用——但项目里仍保留 `hostEnv.ts` 的 `getHostWindow()`/`getHostDocument()` 调用点（接口保留、实现天然退化，见 decision 0010），新代码可以直接用裸引用。

## 入口

入口在 `main.ts` 的 `registerEntries()`（由 `init()` 调）里注册：
- 斜杠命令 `/workbench`：宿主全局 `SlashCommandParser.addCommandObject` 注册，聊天框敲命令触发。
- ST 顶栏按钮：幂等 id `st-wb-entry-button` 挂到 `#extensionsMenu`（兜底 `body`）。
- 两条入口都派发 `st-workbench:open-panel` 自定义事件到 `window`，`App.vue` 在 `onMounted` 挂监听触发 `openPanel()`——事件名是 `main.ts` ↔ `App.vue` 的契约，两侧需一致。

## 挂载点本身长什么样

（`main.ts`）：显式给了一套完整的定位样式，直接内联写在元素上（不依赖 CSS 层叠，避免被任何后加载的规则打乱）：`position:fixed; top:0; left:0; width:100vw; height:100dvh(回退100vh); z-index:2147483647`（合法 z-index 最大值）。这个元素 append 到 `document.body`（不是 `<html>`）。挂载后还有一段一次性诊断：检测到宿主 `<html>` 上有非 `none` 的 `transform`/`perspective`/`filter`/`will-change: transform` 时 `console.warn`——这类属性本身不影响 `vw`/`dvh` 的尺寸计算，但如果 transform 真的包含位移，会让 `top:0;left:0` 偏离真实视口角落。

**全局错误边界**：`app.config.errorHandler` 捕获任何组件 setup/render 抛错或 watcher 异常——本扩展注入到宿主页面顶层 `<body>`，未捕获错误会导致整个面板白屏、甚至破坏宿主页面布局。handler 里至少 `console.error` 出来，再往顶层文档叠一个红色 banner 提示"出错了、建议刷新"，点击关闭——避免用户看到一片空白还以为是宿主页面的 bug。此时 Pinia store 可能尚未初始化（错误发生在 `app.mount` 之前），所以不能依赖 `uiStore.showToast`，只能用最原始的 DOM。

**脚本卸载清理**：标准扩展没有"扩展级卸载"事件，页面生命周期即扩展生命周期。`main.ts` 监听 `window` 的 `pagehide`/`unload`（`pagehide` 在 bfcache 关闭时最可靠，`unload` 作旧浏览器兜底，都用 `once:true` 避免重复卸载），触发时 `app.unmount()` + 根容器 `el.remove()`——CSS 由 manifest `css` 字段注入，不再有手动注入的 `<style>` 要清理。

## 模态化面板

迁移后走模态 dialog 路线（见 decision 0010）：根容器不再 `pointer-events:none`，而是吃掉所有点击做遮罩；`.wb-panel` 铺满视口做编辑面，`::before` 伪元素给半透明遮罩视觉。关闭态用 `#ST_Workbench:not(:has(.wb-panel))` 让根容器在面板关时穿透回宿主页面（入口按钮照常可点）。

## hostEnv 纪律

`hostEnv.ts` 接口保留（`getHostWindow()`/`getHostDocument()` 在标准扩展下天然退化为裸 `window`/`document`，调用点零改动即可工作），新代码可以直接用裸 `window`/`document`。`copyToHostClipboard()` 的 `execCommand` 兜底链、`useIsMobile()` 的 `matchMedia` 逻辑仍保留——这些不是 iframe 语义，是真实可用的兜底。原生 `window.confirm/prompt/alert` 在 TauriTavern 的 WebView2 宿主里不可靠，一律走 `confirmStore.ask()`/`askInput()`。

## CSS 与层叠上下文约束

- **`<html>` 有 `transform`（即使是 identity matrix）时，`position:fixed` 元素的 containing block 会变成 `<html>` 而不是 viewport**，导致 `inset:0`/`bottom:24px` 等计算错误、UI 滑到视口外。修复：挂载点用 `100vw`/`100dvh`（视口单位，不走 containing-block 链路）显式给宽高，不用 `inset:0`；内部浮层全部 `position:absolute` 锚定到这个挂载点。
- **移动端 ST 主题把 `<body>` 做成 `position:fixed`**（防止软键盘弹出时页面抖动），且 `position:fixed` 会无条件建立新的层叠上下文——这意味着 `<body>` 自己变成了一个层叠上下文，包含聊天输入框等元素；一个"DOM 顺序更靠后但自身没有 positioned"的裸 `<div>`，画在"in-flow 非定位"这一层，天然低于任何 positioned 层叠上下文，跟 DOM 顺序完全无关。修复：挂载点显式给了 `position:fixed` + 具体数值的 `z-index`（`auto` 的 z-index 虽然也建立层叠上下文，但排在"z-index 0/auto"这一层，实测不足以稳定盖过 `<body>` 自己的层叠上下文；显式正数 z-index 才进入更高的"positive z-index"层，无条件盖过前者）；append 到 `<body>`（不是 `<html>`），让自己的 z-index 在 `<body>` 的层叠上下文*内部*竞争，而不是站在外面跟整个 `<body>` 比。
- **`100vh` 在移动端会被底部导航栏/地址栏压缩**，导致内容被裁。修复：挂载点高度用 `100dvh`（dynamic viewport height），不支持 `dvh` 的浏览器自动回退到 `100vh`（先设 `height:100vh` 再覆盖设 `height:100dvh`，两行都写，靠属性覆盖顺序实现"降级不报错"）。

酒馆本身是个大页面，没有 Shadow DOM 隔离，所有样式共享同一个层叠上下文：

1. **选择器优先级算错**：基础规则意外压过专门给编辑器写的覆盖规则。用祖先类名限定（如 `.wb-editor-wrap .wb-editor-ta`）确保优先级稳赢。
2. **裸 `input` 选择器连 checkbox 一起选中**：写 `.wb-form input{width:100%}` 没限定 type，把 checkbox 也撑成整条横线。写 `.wb-form .wb-form-textarea{width:100%}` 只选中文本类输入框，checkbox 另外单独 `width:auto`（形状见「ui样式与组件字典」表单类）。**同类规则以后再写，先想清楚会不会连累到 checkbox/radio**。
3. **类名太通用导致被外部样式撞车**：语法高亮相关类名要加 `.st-wb` 前缀。
4. **重构 DOM 结构后 CSS 选择器没跟着改**：选择器语法合法但匹配不到任何元素，安静失效。**重构 DOM 结构时必须同步检查所有引用了旧 class 名的 CSS 选择器**。
5. **宿主页面（特别是移动端主题）的全局 `button` 重置会覆盖我们的样式**：关键视觉样式加 `!important`（`background`/`border`/`pointer-events`）。
6. **移动端断点规则要赢过桌面端的 `.float` 修饰**：`.wb-right-panel`/`.wb-preview-panel` 的 `.float` 变体是在 `@media` 外面定义的（同样 `position:absolute`），如果移动端断点内的规则选择器特异性不够，用户开着悬浮模式进入手机布局时会看到桌面端悬浮样式而不是底部 sheet。做法是断点内**显式复合选择器同时匹配 `.float` 变体**（`.wb-right-panel.float` 而不是只写 `.wb-right-panel`），配合媒体查询内规则总是后声明的源码顺序，稳赢桌面端规则。
