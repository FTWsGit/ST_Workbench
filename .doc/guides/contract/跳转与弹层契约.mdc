---
name: "跳转与弹层契约"
kind: "contract"
description: "查 var-click 事件协议、跨域跳转共享契约时读"
alwaysApply: false
---

# 跳转与弹层契约

## var-click 事件协议

`HighlightedEditor` 上的变量点击事件协议（`HighlightedEditor.vue` 发，三域编辑器接）：

```
// HighlightedEditor 上加两个东西：
enable-var-click          // prop（点中变量宏名时发 var-click，默认关）
@var-click="onVarClick"   // 事件

// 事件负载：
emit('var-click', { varName, scope, cursorPos, pos })   // 命中变量宏名
emit('var-click-miss')                                  // 未命中

// onVarClick 路由到 uiStore.showVarPopup（跨域 useVarNav 整套只挂在 uiStore，ContentEditor 直接 import uiStore 复用）
// payload 已知字段：clickDomain / clickBlockId
```

## tabsStore.editorJump 跨域共享契约

三个域的 ContentEditor 都接同一份 `:jump=tabsStore.editorJump`。`HighlightedEditor` 的 `watch(props.jump)` → `moveCursorTo(line, col, len)`，光标 select 高亮在跨域跳转时都生效。

## jumpToFieldHit

工具箱通用 Search 的"跳到命中"出口，preset/worldbook/character 三个 store 各实现一份：

```
jumpToFieldHit(itemId, fieldKey, line, col, len)
```
