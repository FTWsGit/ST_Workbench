---
name: "API层"
kind: "subsystem"
description: "查工作层 api 层与 ST 原生模块/端点的契约、字段重命名与转换时读"
alwaysApply: false
---

# 工作层 api 契约

`src/api/` 三个文件是对 ST 原生模块的封装层，也是「原生 ⇄ 干净」的**唯一转换边界**。函数签名直接读 `.ts` 源码；本篇只记录类型表达不了的 boundary gotcha 与字段映射表。

干净层类型见 `types.ts`（`Preset`/`PresetSettings`/`PromptBlock`/`RegexScript`/`Script`/`Character`/`Worldbook`/`WorldbookEntry`）。干净层只建模 workbench 需要编辑的字段；未建模的 ST 原生字段由 store 持有一份原生 raw 快照（preset/worldbook 是 `store.rawData`，character 是 `store.oldRaw`），保存时经 `toNative*` 的 raw 参数原样透传，保证读-改-存不丢数据。

## characterApi.ts

- 写侧 HTTP multipart POST `/api/characters/create|edit`，顶层窗口 `fetch()` 发起（iframe 自己的 fetch 会按假 origin 解析相对路径）。
- 读侧顶层窗口 import `/script.js` 拿 `characters` 活引用（ESM live binding）。
- `buildFormData` 的 `oldRaw` 必须是 `getCharacterByAvatar()` 返回的 raw，不能凭空构造——编辑时回传 `oldRaw.chat`/`oldRaw.create_date`；新建时（`oldRaw=null`）不传。
- `avatar` 只在头像真换时携带。
- `extensions` 以 `oldRaw.extensions` 打底、工作层已知字段覆盖；`tavern_helper.variables` 不建模，`{ ...oldTH }` 原样透传。
- `fromRaw` 产出新 Character（`description` 顶层 + `otherPrompts`/`creatorMeta` 嵌套 + `regexs`/`scripts`）。

## presetApi.ts

- 统一走 `PresetManager('openai')`（无参版本解析到当前 UI 主 API，可能不是 chat-completion）。
- `getPresetByName(name)` 返回 `{ preset: 干净 Preset, raw: 原生快照 } | null`——raw 供保存时字段级透传。
- `savePresetAs(name, preset, raw, hiddenBlocks?)`：`preset` 是干净 Preset，`raw` 是原生快照（透传未建模字段），`hiddenBlocks` 是工作层隐藏块（不进 prompt_order 但数据写回，覆盖 raw 里的旧数据）。
- 干净层把原生 `prompts` + `prompt_order` 两套数组合并成一份 `prompts`（数组顺序 = 视觉顺序，enabled + 分组字段烘进每条 block）；隐藏块（原生 prompts 里、prompt_order 未引用的）由 `extractHiddenPromptBlocks(raw)` 单独取回。

## worldbookApi.ts

- 顶层窗口 import `/scripts/world-info.js`，状态在模块实例内部，不发 HTTP。
- `getWorldbookByName(name)` 返回 `{ worldbook: 干净 Worldbook, raw: 原生 entries 快照 } | null`；`saveWorldbook(wb, raw?)`。
- 字段转换在边界：`fromSTEntry` 把原生扁平字段重组成 strategy/position/recursion/effect；`toSTEntry` 反向 + raw 透传未建模字段。
- `displayIndex` 仍作为原生持久化字段写回（数组顺序 → displayIndex），但不进干净层。

## 工作层字段重命名与转换

干净层与 ST 原生之间的字段名/结构差异只在 `src/api/` 边界转换，store/组件只认干净层。

### 世界书（WorldbookEntry：原生扁平 → 干净嵌套）

| 干净层 | ST 原生 | 说明 |
| --- | --- | --- |
| `name` | `comment` | |
| `enabled` | `disable` | 取反 |
| `strategy.type` | `constant`/`vectorized`/`selective` 三 bool | 'constant' ⇄ constant=true；'vectorized' ⇄ vectorized=true；'keyword' ⇄ selective=true（三选一，keyword 是兜底） |
| `strategy.keys` | `key` | |
| `strategy.keysSecondary.keys` | `keysecondary` | |
| `strategy.keysSecondary.logic` | `selectiveLogic` | 0=and_any / 1=not_all / 2=not_any / 3=and_all |
| `strategy.scanDepth` | `scanDepth` | 原生 null ⇄ 'same_as_global' |
| `strategy.caseSensitive`/`matchWholeWords` | 同名 | boolean \| null |
| `position.type` | `position` | 数字 0~7 ⇄ 字符串枚举（见 types.ts WORLDBOOK_POSITION_OPTIONS） |
| `position.role` | `role` | system/user/assistant/null ⇄ 0/1/2/null |
| `position.depth`/`order` | 同名 | |
| `recursion.preventIncoming` | `excludeRecursion` | |
| `recursion.preventOutgoing` | `preventRecursion` | |
| `recursion.delayUntil` | `delayUntilRecursion` | 数字透传；布尔 → null |
| `effect.sticky`/`cooldown`/`delay` | 同名 | |

未建模字段（`group`/`groupOverride`/`groupWeight`/`useProbability`/`ignoreBudget`/`addMemo`/`outletName`/`automationId`/`triggers`/`match*` 等）经 raw 透传。

### 预设（Preset）

| 干净层 | ST 原生 | 说明 |
| --- | --- | --- |
| `preset.prompts`（单数组，顺序=视觉顺序） | `prompts` + `prompt_order` 两套 | 每条 PromptBlock 烘入 `enabled` + 分组字段（来自 prompt_order 条目）；injection_position/depth/order → injectionPosition/injectionDepth/injectionOrder |
| `preset.settings` | 顶层散字段 | 只建模 14 个采样参数；其余（模型名/formats/names_behavior 等）raw 透传 |
| `preset.regexs` | `extensions.regex_scripts` | `disabled` ⇄ `enabled` 取反 |
| `preset.scripts` | `extensions.tavern_helper.scripts` | 扁平 Script[]，原生 ScriptFolder 折叠成组（_gid=folder.id，写回重建 folder，icon/color 用默认） |

### 角色卡（Character）

| 干净层 | ST 原生 | 说明 |
| --- | --- | --- |
| `description`（顶层） | v2.description / v1.description | |
| `otherPrompts.*` | v2 同名字段 | scenario/mesExample/personality/systemPrompt/postHistoryInstructions/depthPrompt；depthPrompt.role 字符串枚举与 ST 原生一致，兼容历史数字值兜底 |
| `creatorMeta.*` | v2.creator / creator_notes / character_version / tags | creatorNotes 兜底 v1.creatorcomment |
| `regexs`/`scripts` | extensions.regex_scripts / tavern_helper.scripts | 同预设 |
| `worldbook` | extensions.world | undefined → null |

分组持久化字段（`_gid`/`_gname`/`_gcollapsed`/`_genabled`/`_gidx`）在干净层 PromptBlock/RegexScript/Script/WorldbookEntry 上显式建模，是分组树的持久化载体（ST 原生忽略这些未知字段）。
