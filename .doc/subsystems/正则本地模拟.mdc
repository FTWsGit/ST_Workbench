---
name: "正则本地模拟"
kind: "subsystem"
description: "regexEngine.ts：正则脚本测试栏的本地模拟。理解本地模拟能力边界与限制、排查'正则预览不准/对不上'类症状时读。跟 Precise Preview 是两套不相关的机制。"
alwaysApply: false
---

# regexEngine.ts：正则预览的本地模拟

## 能力边界与限制

`applyRegexScript()` 只做查找/替换/修剪的本地模拟：不解析 `substituteRegex`——匹配前的宏替换需要真实 ST context，本地模拟迟早会跟真实的对不上；不体现 `placement`/`minDepth`/`maxDepth`——这几个是"这条消息在聊天记录里第几层/属于哪类消息"的上下文概念，单独一段贴进来的测试文本没有这个信息。

## 为什么 UI 要明示"预览≠真实"

UI 上要明确提示这个限制（RegexContentEditor 测试栏下方那行小字），不能让用户误以为预览等于真实效果。
