---
name: "agent"
kind: "subsystem"
description: "Agent 子系统：一个跨 preset/worldbook/character 三个 store 的运维助手。这里讲解agent系统的架构、调用API机制和所有跟Agent有关的部分"
alwaysApply: false
---

# agent

Agent 子系统是横跨 preset/worldbook/character 三个 store 的持续对话式运维助手视图（UI 右侧栏 `AgentPanel.vue` + `agentStore.ts`）。改 agent/、agentApi.ts、AgentPanel.vue、agent 工具注册时读。

## 架构分层

```
驱动层  AgentPanel.vue          ← 用户输入、消息渲染、状态反馈
   ▼
运行时层  agentStore.ts         ← 唯一 owner：会话状态机、持久化、LLM 调用入口
   ▼
核心循环层  runAgentTurn        ← 工具调用循环，与业务零耦合
   ▼
能力层  tools/ + toolRegistry  ← 工具注册表、只读/写类/测试创作工具集
```

依赖方向**单向向下**，禁止反向引用。

## 关键文件（src/agent/ 图）

| 文件 | 职责 |
|---|---|
| `src/agent/agentStore.ts` | Pinia store（id `'agent'`），持有 `AgentPersisted` live 状态 + 运行时状态机；唯一调用 LLM 生成与工具执行循环的入口；内嵌审批门；组装分块 system 消息（`buildSystemMessages` + `refreshRuntimePrompt`）；不直接操作 `presetStore`/`worldbookStore`/`characterStore` 的内部字段——所有跨 store 操作必须经过工具注册表 |
| `src/api/agentApi.ts` | agent 持久化读写层，走 `extensionSettings`（服务端 `settings.json` 同级），跟 `characterApi.ts`/`presetApi.ts` 同一套 `ensureTopImporter()` 顶层动态 import 模式 |
| `src/agent/callModel.ts` | LLM 调用层：监听 `CHAT_COMPLETION_SETTINGS_READY` 事件，在请求体组装完成、即将发 API 之前把 `tools` 塞进去；`generateRawData` 返回原始 response object |
| `src/agent/toolCallCompat.ts` | 极简 tool-call 提取层（非流式，OpenAI/Claude/Cohere 三族） |
| `src/agent/toolRegistry.ts` | agent 专用工具注册表（JSON Schema + risk 分级 + execute 函数）|
| `src/agent/tools/readonly.ts` | 只读工具集（`risk:'safe'`） |
| `src/agent/tools/write.ts` | 写类工具集（`risk:'risky'`） |
| `src/agent/tools/preview.ts` | 测试/创作工具集 |
| `src/agent/contextManager.ts` | 上下文管理：神圣前缀（压缩永不触碰）+ 入库截断 + 整段摘要压缩 |
| `src/agent/constants.ts` | agent 子系统的可调常数集中表（禁止散落到各文件当魔法数字） |
| `src/agent/types.ts` | agent 类型定义（`Message`/`ToolCall`/`AgentPersisted`/`AgentConfig`/`AgentSessionMeta`/`AgentRuntimeState`） |
| `src/components/shared/AgentPanel.vue` | Agent 助手右侧栏，三态形态（docked/overlay/float，`settings.agentMode`）；面板内有可展开设置区（⚙ 按钮）：system prompt（绑 `config.prompts.system`）+ temperature/maxTokens/topP/topK/presencePenalty/frequencyPenalty/thinking/maxContextTokens/compactThresholdRatio 全部可配字段，变更走 `agentStore.updateConfig()` 持久化；并渲染内嵌审批卡片（`pendingApproval` + "本会话自动同意"） |

## 持久化机制

走 SillyTavern 官方给扩展预留的 `extensionSettings` 通道（服务端 `settings.json`），不依赖 localStorage。命名空间键唯一不通用（`ST_Workbench_Agent`）。读写纪律跟预设/角色卡一致：写入前 `deepClonePlain()`，读出来也过一次 `deepClonePlain()`，绝不把 Pinia 响应式对象塞进 `extension_settings`。

版本迁移：`AgentPersisted.version` 不匹配已知版本时，**显式报错并给用户一个"重置 agent 数据"的按钮**，禁止静默按新结构误读旧数据、也禁止 silently 清空。

## LLM 调用机制

纯 ST 原生 API（`/script.js` + `/scripts/extensions.js`），不依赖 Tavern Helper。对策是自己在 ST 原生的更底层原语上重建一层"只拿结构化 `tool_calls`、不自动执行、不写 chat"的调用层：

1. 监听 `CHAT_COMPLETION_SETTINGS_READY` 事件：在 `generateRawData()` 触发这个事件的那一刻把 `tools` 塞进去。
2. 调用 `generateRawData({ prompt: renderMessages(store.messages) })`，拿到原始 response object。
3. 自己写极简版 tool-call 提取函数（`toolCallCompat.ts`），从 response 里挖 `tool_calls`。

`tool` 角色编码：我们自己组装的是发给 provider 的原始请求消息数组（OpenAI wire format），`role: 'tool'`（配 `tool_call_id`）是这个格式原生支持的字段。`renderMessages()` 序列化时按 4 角色原样映射。

System 消息组装：分块（system/project/workflow/knowledge/runtime）在每轮调用前由 `buildSystemMessages()` 各生成一条独立的 `{role:'system'}` 消息，前置到会话消息之前；knowledge 只注入 `enabled:true` 的块；runtime 块由 `refreshRuntimePrompt()` 在每次调用前按 tabsStore/presetStore/worldbookStore/characterStore 当前状态刷新（当前 workspace 打开了什么文档）。

## 工具调用循环

```ts
async function runAgentTurn(store, userText) {
  store.messages.push(userMessage(userText))
  maybeAutoCompact(store)          // 每轮调用前触发摘要压缩

  for (let round = 0; round < MAX_TOOL_ROUNDS; round++) {
    const result = await callModel(store)   // generateRawData + 手动注入 tools

    if (!result.toolCalls || result.toolCalls.length === 0) {
      store.messages.push(assistantMessage(result.content))
      return finalize(store, 'complete')
    }

    store.messages.push(assistantMessage(result.content, result.toolCalls))
    for (const call of result.toolCalls) {
      const outcome = await executeTool(store, call)   // 含审批门 + 入库截断
      store.messages.push(toolResultMessage(call.id, outcome))
    }
  }
  return finalize(store, 'max_rounds_exceeded')   // 熔断
}
```

- 只读工具可并行（`readonly: true`），写类工具串行；
- `MAX_TOOL_ROUNDS` 熔断：防止模型陷入调用循环；

## 上下文管理

`contextManager.ts` 管神圣前缀、入库截断、整段摘要压缩三件事。

### compact 摘要压缩——只有两个触发

1. `usedContext/maxContextTokens > compactThresholdRatio`：用户配的百分比，无固定 token 阈值 fallback（默认 `maxContextTokens=256000` / `compactThresholdRatio=0.7`，ratio 不合法时跳过等 overflow）。
2. overflow 兜底：`estimateBytes >= ACTIVE_SESSION_SOFT_LIMIT_BYTES`（256 KiB）触发强制摘要压缩。

compact 分两路：user/assistant 喂 LLM summary（重试 `COMPACT_MAX_RETRIES`=3 次、超时 `SUMMARY_TIMEOUT_MS`=60s、`<previous_summary>` tag 包裹、已有摘要叠加而非蒸馏）；tool_result 按体积折叠或原样保留不进 summary（折叠阈值 `SUMMARY_TOO_BIG_PREFIX`=2 KiB）。合成 `system` 消息 role。overflow 兜底也走 `compactMessages` 而非占位替换。`countTokensAsync` 走 ST tokenizer 精确计数，拿不到 ctx 回退字节估算（`TOKEN_BYTES_ESTIMATE`=4）。

### 入库截断

所有 tool_result 写入 `messages` 前过字节上限（`TOOL_RESULT_TRUNCATE_BYTES`=8 KiB），跟 contextManager 的入口闸门是同一个纪律。

### 审批停止

写类工具审批被拒绝时工具结果记为 `isError:true` 的 `tool` 消息并置 `stopTurn:true`——`runAgentTurn` 看到 `stopTurn` 直接 break 工具循环、finalize 成 `'canceled'`，不再让模型续跑工具或续答。

## 安全与审批边界

### 风险分级 + 审批门

写类工具会真正改用户的预设/角色卡/世界书数据，模型的判断不能直接绕过用户。内嵌卡片（不弹全局模态）是为了不打断其它操作，"本会话自动同意"是在"每次都问"和"完全信任"之间给用户一个折中挡位。

方法形状（`requestApproval`/`resolveApproval`/`pendingApproval`/`autoApprovedTools`）直接读 `src/agent/agentStore.ts` 源码。

### 统一 tools（不分 workspace）

所有工具一次性全导进 `tools` 数组发给模型，不按当前 workspace 过滤。`AgentToolDef.availableIn` 字段、\agent 跨 preset/worldbook/character 三个 store 运维，工具全集常驻一次到位，避免按 ws 分导致的 provider 端 tools 缓存不命中

### 分页语义

三个 get 工具支持 `offset`/`limit` 分页（1-based 行号），`sliceLines` 切片后附 "showing lines X-Y of N, call again with offset Z" 翻页报告，避免长内容被 truncate 锁死读不到。

### 提示注入防护

三条防线，在本产品里格外重要——**工具返回的 preset/worldbook/角色卡内容是用户自己写的创作文本，完全可能包含"忽略之前的指令"这种文字**（用户写小说梗概时太正常了）。摘要 framing、tool 结果 framing（`frame()` 函数）都不能省。

## 生命周期状态机（简化版）

```text
idle ──submit──► thinking ──tool_call──► tool_loop ──…──► turn_complete ──► idle
                  │                         │
                  └──cancel──► idle         └──error──► idle（错误消息追加进 messages，不清空会话）
```

每个状态有明确的 UI 反馈（`AgentPanel.vue` 显示 spinner/当前在调用哪个工具）。

## 会话切换

`AgentPersisted.sessionMessages: Record<string, Message[]>` 按 session id 存各会话消息历史。`switchSession(id)` 先把当前 `activeSessionMessages` 写回 `sessionMessages[oldId]`，再从 `sessionMessages[id]` 加载目标会话消息——切换不丢消息。`newSession` 同样先把当前消息存到 `sessionMessages[oldId]` 再建新 session。`deleteSession(id)` 删 sessions 索引 + 删 `sessionMessages[id]`；删的是活跃会话时 `activeSessionId = null`。`trimSessions` 丢最旧会话索引时同步删 `sessionMessages` 里对应的 id，避免 orphan 消息累积。

UI（`AgentPanel.vue` 会话头）：`<select>` 下拉列所有 sessions（按 `updatedAt` 降序，title 截断 30 字符）+ "＋"新建 + "🗑"删除当前活跃会话。删除走 `confirmStore.ask()` 确认（不用原生 confirm）。切换器 `v-if="agentStore.sessions.length > 0"`——只要还有任何 session 就显示，删掉当前活跃会话后也能切换到其他会话。

## Token 用量显示

`AgentPanel.vue` 状态条右端显示 `usedContext/maxContext`。`usedContextTokens` ref 由 `refreshTokenCount()` 异步更新（调 `contextManager.countTokensAsync`，走 ST tokenizer 精确计数，拿不到回退字节估算）。两个 watch 触发重算：`activeSessionMessages` deep watch（消息变化）、`activeSessionId` watch（切换会话）；`onMounted` 算一次。`maxContextDisplay` computed：`config.maxContextTokens > 0` 时显示数值，为 0 时显示 `'?'`（0 表示用默认阈值）。

## 工具 description 管理

三个 tools 文件（`readonly.ts`/`write.ts`/`preview.ts`）顶部各有一个 `const TOOL_DESC = { ... } as const` 集中管理所有工具描述文案，英文 atomcode 风格（做什么/不做什么/边界/何时用/参数语义/返回形态/错误边界/反例陷阱，150-250 字符），`registerAgentTool` 里 `description` 字段引用 `TOOL_DESC.xxx`。改描述改 const 即可，便于 diff 评审。
