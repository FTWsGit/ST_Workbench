---
name: "0005-preview-syncs-st-main-menu-selection"
kind: "decision"
description: "为什么 Preview 生成前要显式把 ST 主菜单选中的预设和角色卡切到当前编辑的这份，以及为什么只在生成前调用一次而不是保持实时同步"
alwaysApply: false
status: "accepted"
supersedes: ""
---

# 0005. Preview 生成前显式同步 ST 主菜单选中的预设和角色卡

## 背景

`ST.generate('normal')`（Precise Preview 的"完整请求原文"模式、Preset Preview 走的
方案 B 单例）不认工具里正在编辑的是哪份预设、哪张角色卡，只认 ST 主菜单当前选中的
是哪份预设、哪张角色卡。如果用户在本工具里切到了另一份预设/另一张角色卡编辑，但 ST
主菜单还停在原来那份，Preview 生成出来的会是主菜单那份的内容，跟用户在编辑器里看到
的对不上。角色卡这一面尤其隐蔽：工具编辑器里的角色卡改动并不自动反映到 ST 主菜单选
中项，不显式对齐就会用错角色卡渲染。

## 决策

`ST.selectPresetByName(name)` 和 `mod.selectCharacterById(chid, { switchMenu: false })`
都能切主菜单选中项，但这两个操作在 ST 里很慢，所以不做成"跟编辑器实时保持一致"，
只在 `PreviewPanel.vue` 点"生成"之前依次调用一次 `store.selectPresetByName(store.presetName)`
和 `characterStore.selectCharacterForPreview()`（后者封装 `selectCharacterByAvatar`，
内部按 `avatar` 反查 `chid`）。两个函数都内部先判断"ST 主菜单当前是否已经是这份"，
是则跳过、零开销——只有真不一致时才付这一次代价。切换失败走对应 toast
（`shared.toast.selectPresetFailed` / `character.toast.selectCharFailed`）提示"可能导致
显示数据不精确"。

## 后果

- 用户在本工具里频繁切换预设/角色卡编辑、但不点生成预览，完全不触发这次同步，没有
  性能代价。
- 如果以后出现别的功能也依赖"ST 主菜单选中项要跟编辑器一致"，优先复用
  `store.selectPresetByName` / `selectCharacterForPreview`，不要重新发明一个实时同步的
  watcher——那会把这次刻意换来的"只在真正需要时才付一次代价"的设计破坏掉。
