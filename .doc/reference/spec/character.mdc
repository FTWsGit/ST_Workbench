---
name: "character"
description: "查 ST 角色卡原生字段形状、ST 原生读写模块签名"
kind: "spec"
alwaysApply: false
---

# character

## 数据结构

角色卡是 `.png` 文件（文件名 = 角色名，`avatar` 字段是唯一 id）。前端内存里是 `v1CharData`（顶层 V1 遗留字段 + 服务端附加字段），V2 规范的数据都在 `data` 里。

```ts
type Character = {
  // —— 顶层（V1 遗留 + 服务端附加）——
  name: string;
  description: string;       // 旧版正文（新规范以 data.* 为准）
  personality: string;
  scenario: string;
  first_mes: string;
  mes_example: string;
  creatorcomment: string;    // 旧版创建者备注
  tags: string[];
  talkativeness: number;       // 旧卡可能是字符串，读取后由 extensions.talkativeness 规范成 number
  fav: boolean;                // v1 旧卡可能是 boolean | string
  create_date: string;
  chat: string;              // 当前对话文件名（服务端附加）
  avatar: string;            // '<名字>.png'，唯一 id（服务端附加）
  json_data: string;         // 完整原始 JSON（服务端附加）
  date_added: number;        // 文件创建时间（服务端附加）
  date_last_chat: number;    // 最近聊天时间（服务端附加）
  chat_size: number;         // 聊天文件大小（服务端附加）
  data_size: number;         // 卡数据大小（服务端附加）
  shallow?: boolean;         // 懒加载标记：true 时 data 只有浅层字段

  // —— V2 卡片数据 ——
  spec?: string;             // 'chara_card_v2'（文件顶层，内存对象里保留）
  spec_version?: string;     // '2.0'
  data: {
    name: string;
    description: string;
    character_version: string;
    personality: string;
    scenario: string;
    first_mes: string;
    mes_example: string;
    creator_notes: string;
    tags: string[];
    system_prompt: string;
    post_history_instructions: string;
    creator: string;
    alternate_greetings: string[];    // first_mes 之外的额外开场白
    character_book?: CharacterBook;   // 内嵌角色书（展开见下方「CharacterBook」）

    extensions: {
      talkativeness: number;
      fav: boolean;
      world: string;                  // 绑定的世界书名
      depth_prompt: { depth: number; prompt: string; role: 'system' | 'user' | 'assistant' };
                                      // ST_Workbench 工作层 Character.depthPrompt.role 直接用字符串枚举，与 ST 原生一致
      regex_scripts: RegexScript[];   // 正则脚本：12 字段 + [k: string]: any
      // Tavern Helper 注入：绑定到该角色卡的脚本与面板变量
      tavern_helper: {
        scripts: ScriptTree[];
        variables: Record<string, any>;
      };
      // 其他扩展键（pygmalion_id / github_repo / chub ...）可继续加
    };
  };
};
```

### CharacterBook

```ts
// 内嵌角色书（character_book）：字符卡 V2 规范的世界书条目
type CharacterBook = {
  name: string;
  entries: {
    id: number;               // 条目 id（ST 依赖它反查原数据）
    keys: string[];           // 触发词
    secondary_keys?: string[]; // 次要触发词
    content: string;
    enabled: boolean;
    insertion_order: number;  // 排序
    position: string;         // 'before_char' | 'after_char' | ...
    use_regex?: boolean;      // 是否按正则匹配
    comment?: string;
    constant?: boolean;
    selective?: boolean;
    extensions?: Record<string, any>;
  }[];
};
```

## 读写

以下接口都来自 ST 前端自身导出的模块，用标准动态 import 按路径加载即可（路径即文件在 `public/` 下的位置）。

### 读取

| 函数 | 参数 | 返回 | 语义 |
| --- | --- | --- | --- |
| `getCharacters`（`/script.js`） | 无 | `void`（填充全局 `characters`） | 拉取全部角色卡（懒加载：shallow 卡片只有浅层字段） |
| `getOneCharacter`（`/script.js`） | `avatar: string`（'名字.png'） | `void`（替换 `characters` 中对应项） | 按头像名从服务端重拉完整卡 |
| `getCharacterCardFields`（`/script.js`） | `{ chid: string }` | 编辑面板解析字段（system/mesExamples/description/... 已拼好） | 读取编辑面板所需的解析字段 |
| `printCharacters`（`/script.js`） | `force?: boolean` | `void` | 重绘角色列表 |
| `getContext`（`/scripts/extensions.js`） | 无 | `{ eventSource, event_types }` | 取事件总线；`eventSource.on(event_types.CHARACTER_EDITED, cb)` 订阅角色卡编辑保存事件 |

`this_chid` 是 `characters` 数组下标（字符串），当前选中角色 = `characters[this_chid]`。

### 选中 / 切换主菜单选中项

| 函数 | 参数 | 返回 | 语义 |
| --- | --- | --- | --- |
| `selectCharacterById`（`mod`，`/scripts/quickReply.js` 里由 ST 暴露的 `getContext().selectCharacterById`） | `chid: number`, `opts?: { switchMenu?: boolean }` | `void` | 切 ST 主菜单选中角色。`chid` 是 `mod.characters` 数组下标。`switchMenu: false` 表示只切角色不切 ST 顶层菜单标签页 |

Precise Preview 生成前要调一次这个，把 ST 主菜单选中角色对齐到工具里正在编辑的那张——ST 的 `generate` 只认主菜单选中项，不显式对齐就会用错角色卡渲染（设计见 decision 0005）。本项目封装在 `api/characterApi.ts` 的 `selectCharacterByAvatar(avatar)`，内部按 `avatar` 在 `mod.characters` 里反查 `chid`，已是同一张时直接跳过。

### 缩略图端点

角色头像缩略图走 ST 的 `/thumbnail` 端点：

```
/thumbnail?type=avatar&file=<encodeURIComponent(avatar)>&<可选 cacheBust>
```

`avatar` 是角色卡文件名（含 `.png` 后缀），缓存失效时附一个时间戳查询参数。本项目 `api/characterApi.ts` 的 `getCharacterAvatarUrl(avatar, cacheBust?)` 负责拼这个 URL，`CharacterMetaForm.vue` 直接 `<img :src>` 用它显示原头像。

### 写入

| 函数 | 参数 | 返回 | 语义 |
| --- | --- | --- | --- |
| `saveCharacterDebounced`（`/script.js`） | 无 | `void` | 防抖触发保存：把改过的 `characters` 内存对象持久化 |
| `writeExtensionField`（`/scripts/extensions.js`） | `chid: string`, `key: string`, `value: any \| UNSET_VALUE` | `Promise` | 只写单个扩展字段，自动与旧值合并；传 `UNSET_VALUE` 删除该键 |
| `deleteCharacter`（`/script.js`） | `avatar: string`, `options: { deleteChats: boolean }` | `Promise` | 删除角色（avatar 是文件名而非角色名；`deleteChats` 控制是否一并删聊天） |

> 本项目读写统一走 `api/characterApi.ts` 封装。
