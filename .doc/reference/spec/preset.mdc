---
name: "preset"
description: "查 ST 原生 PresetPrompt / prompt_order 形状、PresetManager 读写入口、savePreset/readPresetExtensionField 签名、ST 预设字段含义时读"
kind: "spec"
alwaysApply: false
---

# preset

## 数据结构

预设是某一类 API（openai 等）生成参数与提示词配置的快照，按名字保存、落盘为 JSON。前端"当前在用"的参数对象本身就是这个形状（叫 `oai_settings`），具名预设是它的快照副本。

```ts
type Preset = {
  // —— 采样/连接参数（数量众多，此处只列稳定代表字段）——
  chat_completion_source: string;   // 当前 API：'openai' | 'claude' | 'custom' ...
  temperature: number;
  top_p: number;
  top_k: number;
  min_p: number;
  repetition_penalty: number;
  frequency_penalty: number;
  presence_penalty: number;
  stream_openai: boolean;
  openai_max_context: number;       // 上下文上限（token）
  openai_max_tokens: number;        // 单次回复上限（token）
  n: number;
  seed: number;                     // -1 = 随机
  names_behavior: number;           // 角色名显示策略
  squash_system_messages: boolean;
  show_thoughts: boolean;
  reasoning_effort: string;
  request_images: boolean;
  function_calling: boolean;
  enable_web_search: boolean;
  // ...各模型专属字段（*_model / *_endpoint / proxy 等）省略。全量字段来源：ST 源码 `public/scripts/openai.js` 的 oai_settings 默认对象。

  // —— 提示词配置 ——
  prompts: PresetPrompt[];                              // 全部提示词
  prompt_order: { character_id: number; order: { identifier: string; enabled: boolean }[] }[];

  // —— 扩展字段 ——
  extensions: {
    // 正则脚本数组（跟预设同存同取，不需单独 IO）
    regex_scripts: RegexScript[];   // 12 字段 + [k: string]: any
    // Tavern Helper 注入：绑定到该预设的脚本与面板变量
    tavern_helper: {
      scripts: ScriptTree[];
      variales: Record<string, any>;  // 面板变量键值对（字段名是 variales，ST 与 ST_Workbench
                                      // 代码里都保留这个拼写；Character 卡的 tavern_helper 才是
                                      // variables）
    };
    // 其他扩展可继续加键
  };
};

// 单个提示词（Prompt 对象，PromptManager 构造；启用/禁用不在 prompt 上，在 prompt_order.order[].enabled）
type PresetPrompt = {
  identifier: string;       // 提示词标识：'main' | 'jailbreak' | 'worldInfoBefore' ...
  name: string;             // 显示名
  role: 'system' | 'user' | 'assistant';
  content?: string;         // 占位提示词无内容，靠 marker 定位
  system_prompt: boolean;   // 是否作为系统提示词
  marker?: boolean;         // 是否占位提示词
  position: string | number;          // 列表内位置
  injection_position: number;         // 0=relative / 1=absolute in-chat
  injection_depth: number;            // in-chat 注入深度
  injection_order: number;            // in-chat 注入顺序
  injection_trigger?: string;         // in-chat 注入触发词
  forbid_overrides?: boolean;
  extension?: Record<string, any>;
};
```

## 读写

以下接口都来自 ST 前端自身导出的模块，用标准动态 import 按路径加载即可（路径即文件在 `public/` 下的位置）。

### 读取（含刷新）

| 函数 | 参数 | 返回 | 语义 |
| --- | --- | --- | --- |
| `import('/scripts/openai.js')` → `oai_settings` | — | `Preset`（"当前在用"参数对象） | 活的引用：改字段即改预设，配合 `saveSettingsDebounced()` 落盘 |
| `import('/script.js')` → `saveSettingsDebounced()` | — | — | 防抖把"在用"参数保存到服务端 |
| `import('/scripts/preset-manager.js')` → `getPresetManager(api)` | `api: 'openai'` | `PresetManager` | 按 API 取具名预设管理器；`ctx.getPresetManager()` 是同一函数（见下文 PresetManager 入口） |
| `pm.getAllPresets()` | — | `string[]` | 全部预设名 |
| `pm.getPresetList()` | — | `{ presets, preset_names, settings }` | 预设列表 + 当前 settings |
| `pm.getCompletionPresetByName(name)` | `name: string` | `Preset` | 指定预设的完整对象（openai 下读取具名预设用这个） |
| `pm.getSelectedPresetName()` | — | `string` | 当前选中的预设名 |
| `pm.readPresetExtensionField({ name, path })` | `path` 支持 `'a.b.c'` | `unknown` | 读预设扩展字段（含 tavern_helper） |
| `import('/scripts/openai.js')` → `promptManager.render()` / `renderDebounced()` | — | — | 改 `prompts` / `prompt_order` 后重渲染提示词面板 |
| `import('/scripts/extensions.js')` → `getContext().eventSource` | — | EventSource | 订阅 ST 事件，如 `event_types.OAI_PRESET_CHANGED_AFTER`（预设已切换） |

### 写入

| 函数 | 参数 | 返回 | 语义 |
| --- | --- | --- | --- |
| `pm.savePreset(name, data, opts?)` | `name: string`，`data: Preset`，`opts?: { skipUpdate }` | `Promise` | 整份替换/保存具名预设；保存预设唯一入口，只覆盖写一个预设文件 |
| `pm.writePresetExtensionField({ name, path, value })` | `path` 支持 `'a.b.c'` | — | 写具名预设扩展字段，自动合并旧值并保存 |
| `pm.deletePreset(name)` | `name: string` | — | 删除预设 |

### PresetManager 入口

`ctx.getPresetManager('openai')` 与 `import('/scripts/preset-manager.js')` 的 `getPresetManager(api)` 是同一个函数。`ctx.getPresetManager()` **不传参数返回的是"ST 当前主 API"对应的 manager**，用户主 API 切走可能根本不是 chat completion，所以保存预设必须显式传 `'openai'`。

本项目侧所有"读/存预设"功能唯一应该调用的入口是 `api/presetApi.ts` 封装，不要绕开直接摸 ST 的 `ctx`：

```
listPresets()
getSelectedPresetName()
getPresetByName(name)
savePresetAs(name, data)
deletePreset(name)
invalidateCache()
selectPresetByName(name)   // 把 ST 主菜单当前选中的预设切换过去
```

### prompt_order 形状

按 `character_id` 定位条目的 TS 形状（与上方 `type Preset.prompt_order` 同源，ST 落盘即此形状）：

```
prompt_order 条目形状 = {
  character_id: number,
  order: { identifier: string, enabled: boolean }[],
}
// 100001 是 ST 里 Chat Completion 全局默认设置的固定 id，按它定位条目
// order 元素引用 PresetPrompt.identifier（'main' | 'jailbreak' | ...），enabled 是启用/禁用开关；
// 不是裸 id 数组。
```

> 本项目读写统一走 `api/presetApi.ts` 封装。
