---
name: "预览与正则模拟"
description: "Precise Preview 真实渲染 ST 管线（非本地模拟）与 regexEngine.ts 正则本地模拟（有意的简化）。改 PreviewPanel/regexEngine/macroAwareDiff/wordDiff/正则预览、或排查'预览不准/trim 不生效/正则无效'类症状时读。selectPresetByName 契约见'状态与持久化'。"
alwaysApply: false
---

# Precise Preview：真实渲染而不是本地模拟

两种预览模式都真实调用 ST 自己的渲染管线，不是自己模拟宏替换（早期方案已淘汰，覆盖不到正则/其它插件的二次加工）。点"生成"前会先调用 `store.selectPresetByName(store.presetName)` 把 ST 主菜单选中的预设对齐到当前工具正在编辑的这份（见「状态与持久化 > 为什么需要切换 ST 主菜单选中项」），已经一致时这一步是零开销的空操作：

1. **按块精确预览**（`getPromptManagerMessages()`）：往顶层文档注入 `<script type="module">` 让 `import('/scripts/openai.js')` 在顶层文档作用域执行，拿到真实的 `promptManager` 单例，跑一次 dry-run 填充它的 `.messages.collection`。
2. **整体原文精确预览**（`getFinalRequestMessages()`）：不能用 dry-run（不会走到"组装完请求体、发给后端 API 之前"这步），走监听 `CHAT_COMPLETION_SETTINGS_READY` 事件 + 跑一次**真实的** `ctx.generate('normal')`，拿到 `completion.messages` 后立刻 `ctx.stopGeneration()` 中断。

## 高亮算法：`macroAwareDiff()`

拿块的原始 `content` 和渲染后文本比较，标出"相对原文新增/被替换"的部分。核心思路：原文按宏边界切成字面量片段，按顺序用只会前进的游标去渲染文本里找每段的原样子串——局部顺序查找，不依赖全局唯一性，天然不会串位。`wordDiff()` 系列没有被删除，是"块里完全没有宏"或"某个片段找不到原样子串"时的兜底。**改这两个函数之前，先看 `utils.ts` 里的多行注释**，改动前脱离 Vue 用 `npx tsx` 单独跑一遍确认没有回归。

## regexEngine.ts：正则预览的本地模拟（有意的简化，不是 bug）

`applyRegexScript()` 只做查找/替换/修剪的本地模拟，**不解析 `substituteRegex`**（匹配前的宏替换需要真实 ST context，本地模拟迟早会跟真实的对不上）、**不体现 `placement`/`minDepth`/`maxDepth`**（这几个是"这条消息在聊天记录里第几层/属于哪类消息"的上下文概念，单独一段贴进来的测试文本没有这个信息）。UI 上要明确提示这个限制（RegexContentEditor 测试栏下方那行小字），不能让用户误以为预览等于真实效果。

`trimStrings` 要同时应用到**完整匹配和每个数字捕获组**。

`parseFindRegex()` 解析 ST 的 `/pattern/flags` 字符串格式（裸 pattern 也兼容），强制带上 `g`（预览要找全部匹配）；解析失败返回 `null` 而不是抛错，UI 显示"正则无效"。
